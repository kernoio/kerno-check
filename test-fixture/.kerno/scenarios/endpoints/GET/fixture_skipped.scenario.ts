import {ctx} from '@kerno/ts-sandbox'

export const meta = {
  description: 'A stub. Must be reported skipped, never passed.',
  path: 'GET /',
}

export default function fixture_skipped() {
  // TODO: implement scenario
  // Guidance: left unimplemented on purpose, to pin the skipped classification
}
